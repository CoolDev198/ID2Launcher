<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/DropTargetBar"
    android:visibility="invisible"
    >
<LinearLayout
   android:orientation="horizontal"
    style="@style/layout">
    <ImageView
        android:id="@+id/remove_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher_clear_normal_holo"/>
    <TextView
        style="@style/DropTargetButton"
        android:id="@+id/remove_target_text"
        android:text="@string/remove_zone_label"
       />

</LinearLayout>

    <!--<TextView-->
        <!--style="@style/DropTargetButton"-->
        <!--android:id="@+id/unistall_target_text"-->
        <!--android:text="@string/uninstall_zone_label"-->
        <!--android:drawableLeft="@mipmap/ic_launcher_trashcan_normal_holo" />-->
</FrameLayout>